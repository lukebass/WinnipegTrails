<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.winnipegtrails.winnipegtrails" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name=".WinnipegTrailsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/wta"
        android:label="@string/app_name" >

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/ActionBar" />

        <activity
            android:name=".SignUpActivity"
            android:label="@string/app_name"
            android:theme="@style/ActionBar" />

        <activity
            android:name=".WebViewActivity"
            android:label="@string/app_name"
            android:theme="@style/ActionBar"
            android:parentActivityName="ca.winnipegtrails.winnipegtrails.SettingsActivity" />

        <activity
            android:name=".EggListActivity"
            android:label="@string/gems"
            android:theme="@style/ActionBar"
            android:parentActivityName="ca.winnipegtrails.winnipegtrails.SettingsActivity" />

        <activity
            android:name=".UserListActivity"
            android:label="@string/leaderboard"
            android:theme="@style/ActionBar"
            android:parentActivityName="ca.winnipegtrails.winnipegtrails.SettingsActivity" />

        <activity
            android:name=".EggActivity"
            android:theme="@style/NoActionBar" />

        <activity
            android:name=".SettingsActivity"
            android:theme="@style/NoActionBar" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBar" />

        <activity
            android:name=".ModeActivity"
            android:theme="@style/NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyANv2LTQB1SxZKZ3AeK_pV6wUaNLdLpAuM" />
    </application>

</manifest>
